// r188 브라우저 적용기 — edits_r188.py 의 OPS. r187(live df8957b9 / test e5c3a297) 에 적용.
function r188Apply(s, isTest){
  function rep(str, old, nw, exp, label){ var n=str.split(old).length-1; if(n!==exp) throw new Error('R188 FAIL '+label+' count '+n+' (expect '+exp+')'); return str.split(old).join(nw); }
  var OPS = [["  function _mmMatchVendor(tokens){\n    var names=allClients().map(function(c){ return String(c[0]||'').trim(); }).filter(Boolean);\n    var lower={}; names.forEach(function(n){ lower[n.toLowerCase()]=n; });\n    for(var n=Math.min(3,tokens.length); n>=1; n--){\n      var cand=tokens.slice(0,n).join(' ').toLowerCase();\n      if(lower[cand]) return { vendor:lower[cand], used:n };\n      var cand2=tokens.slice(0,n).join('').toLowerCase();\n      if(lower[cand2]) return { vendor:lower[cand2], used:n };\n    }\n    var t0=String(tokens[0]||'').toLowerCase(); if(t0.length<2) return null;\n    var hits=names.filter(function(nm){ var l=nm.toLowerCase(); return l.indexOf(t0)===0 || (t0.length>=3 && t0.indexOf(l)===0); });\n    if(hits.length===1) return { vendor:hits[0], used:1 };\n    if(hits.length>1){ hits.sort(function(a,b){ return a.length-b.length; }); return { vendor:hits[0], used:1, ambiguous:true }; }\n    // r185: (주)·㈜·주식회사 등을 떼 핵심 이름으로 맞으면 '연결할까요?' 후보로 돌려준다 (자동 연결은 안 함)\n    var core=function(x){ return String(x||'').toLowerCase().replace(/\\(주\\)|\\(유\\)|㈜|주식회사|유한회사|\\bco\\.?,?\\s*ltd\\.?|\\binc\\.?|[\\s()\\-_.,·]/g,''); };\n    for(var n2=Math.min(3,tokens.length); n2>=1; n2--){\n      var c0=core(tokens.slice(0,n2).join('')); if(c0.length<2) continue;\n      var h2=names.filter(function(nm){ var cn=core(nm); return cn && (cn===c0 || cn.indexOf(c0)===0 || (c0.length>=3 && c0.indexOf(cn)===0)); });\n      if(h2.length){ h2.sort(function(a,b){ return a.length-b.length; }); return { vendor:h2[0], used:n2, suggest:true, rows:_mmVendorRows(h2) }; }\n    }\n    return null;\n  }\n", "  function _mmVendorPool(){   // r188: 업체 탭 + 견적 거래처 + 매입처(브랜드·대리점) 이름을 모두 후보로\n    var out=[], seen={};\n    var add=function(n, src){ n=String(n||'').trim(); if(!n || seen[n]) return; seen[n]=1; out.push({ name:n, src:src }); };\n    (allClients()||[]).forEach(function(c){ add(c[0], 'client'); });\n    (typeof quoteVendors!=='undefined'?(quoteVendors||[]):[]).forEach(function(v){ add(v.name, 'quote'); });\n    (typeof purchaseVendors!=='undefined'?(purchaseVendors||[]):[]).forEach(function(v){ add(v.brand, 'purchase'); add(v.distributor, 'purchase'); });\n    return out;\n  }\n  function _mmMatchVendor(tokens){\n    var pool=_mmVendorPool(); var names=pool.map(function(p){ return p.name; });\n    var lower={}; names.forEach(function(n){ lower[n.toLowerCase()]=n; });\n    for(var n=Math.min(3,tokens.length); n>=1; n--){\n      var cand=tokens.slice(0,n).join(' ').toLowerCase();\n      if(lower[cand]) return { vendor:lower[cand], used:n };\n      var cand2=tokens.slice(0,n).join('').toLowerCase();\n      if(lower[cand2]) return { vendor:lower[cand2], used:n };\n    }\n    // r188: 정확히 같은 이름이 아니면 전부 '연결할까요?' 후보 — (주)·㈜·주식회사 등을 떼 핵심 이름이 같은 것 먼저, 그다음 앞글자 일치\n    var core=function(x){ return String(x||'').toLowerCase().replace(/\\(주\\)|\\(유\\)|㈜|주식회사|유한회사|\\bco\\.?,?\\s*ltd\\.?|\\binc\\.?|[\\s()\\-_.,·]/g,''); };\n    for(var n2=Math.min(3,tokens.length); n2>=1; n2--){\n      var c0=core(tokens.slice(0,n2).join('')); if(c0.length<2) continue;\n      var same=[], pre=[];\n      names.forEach(function(nm){ var cn=core(nm); if(!cn) return; if(cn===c0) same.push(nm); else if(cn.indexOf(c0)===0 || (c0.length>=3 && c0.indexOf(cn)===0)) pre.push(nm); });\n      pre.sort(function(a,b){ return a.length-b.length; });\n      var h2=same.concat(pre);\n      if(h2.length) return { vendor:h2[0], used:n2, suggest:true, rows:_mmVendorRows(h2) };\n    }\n    return null;\n  }\n", 1, "MATCH"], ["  function _mmVendorRows(names){ var seen={}; var out=[]; (allClients()||[]).forEach(function(c){ var nm=String(c[0]||'').trim(); if(names.indexOf(nm)<0) return; var k=nm+'|'+(c[2]||'')+'|'+(c[3]||''); if(seen[k]) return; seen[k]=1; out.push(c); }); return out; }   // r185\n", "  function _mmVendorRows(names){ var seen={}; var out=[]; (allClients()||[]).forEach(function(c){ var nm=String(c[0]||'').trim(); if(names.indexOf(nm)<0) return; var k=nm+'|'+(c[2]||'')+'|'+(c[3]||''); if(seen[k]) return; seen[k]=1; out.push(c); });\n    // r188: 업체 탭에 없고 견적 거래처/매입처에만 있는 이름도 후보 행으로 (주소 없음, 출처 표시)\n    var pool=_mmVendorPool(); names.forEach(function(nm){ if(out.some(function(r){ return String(r[0]).trim()===nm; })) return; var p=pool.find(function(x){ return x.name===nm; }); if(p) out.push([nm,'','','',(p.src==='quote'?'견적 거래처':'매입처')]); });\n    // 입력 순서(핵심 이름 같은 것 → 앞글자 일치) 유지\n    out.sort(function(a,b){ return names.indexOf(String(a[0]).trim())-names.indexOf(String(b[0]).trim()); });\n    return out; }   // r185·r188\n", 1, "ROWS"], ["    if(p.suggestRows && p.suggestRows.length && !line.parent) _mmVendorChoose(line, p.suggestRows, p.suggestTyped);   // r185", "    if(p.suggestRows && p.suggestRows.length && !line.parent) _mmVendorAsk(line, p.suggestRows, p.suggestTyped);   // r185·r188", 1, "CONFIRM"], ["if(mv.suggest || _rw2.length>1){ _mmVendorChoose(l, _rw2, v); return; }   // r185", "if(mv.suggest || _rw2.length>1){ _mmVendorAsk(l, _rw2, v); return; }   // r185·r188", 1, "PICK"], ["  // ── r185: 업체 연결 후보 창 (같은 이름 여러 건 / 비슷한 이름) ──", "  // r188: 후보가 1건이면 확인창(OK → 연결), 여러 건이면 목록 창\n  window._mmVendorAsk = function(ln, rows, typed){\n    if(!rows || !rows.length) return;\n    typed=typed||String(ln.text||'').split(/\\s+/).slice(1,2).join(' ');\n    if(rows.length>1){ _mmVendorChoose(ln, rows, typed); return; }\n    var r=rows[0];\n    var inf={}; try{ inf=_clxInfo(r)||{}; }catch(_e){} var addr=[inf.addr,inf.addr2].filter(Boolean).join(' ');\n    var msg='\"'+_mmEsc(typed)+'\" 을(를) 등록된 업체 <b>'+_mmEsc(r[0])+'</b>'+(r[2]==='화성'?' (화성)':'')+(r[4]?' <span style=\"font-size:11px;color:#6b7280\">['+_mmEsc(r[4])+']</span>':'')+' 로 연결할까요?'+(addr?('<br><span style=\"font-size:12px;color:#6b7280\">'+_mmEsc(addr)+'</span>'):'');\n    showConfirmModal('업체 연결', msg, function(){ ln.vendor=r[0]; ln.vendorBr=(r[2]==='화성'?'화성':''); _mmChildren(ln.id).forEach(function(c){ if(c.kind==='contact') c.vendor=r[0]; }); _mmSave(); renderMemoPage(true); }, '연결', '#1B3A6B');\n  };\n  // ── r185: 업체 연결 후보 창 (같은 이름 여러 건 / 비슷한 이름) ──", 1, "ASK"], ["'</span></div><div style=\"font-size:12px;color:#6b7280;margin-top:2px\">'+(addr?_mmEsc(addr):'주소 미등록')+'</div></div>'; });", "'</span>'+(r[4]?' <span style=\"font-size:11px;color:#b45309\">['+_mtEscSafe(r[4])+']</span>':'')+'</div><div style=\"font-size:12px;color:#6b7280;margin-top:2px\">'+(addr?_mmEsc(addr):'주소 미등록')+'</div></div>'; });", 1, "CHOOSER"], ["  window._mmVendorChoose = function(ln, rows, typed){", "  function _mtEscSafe(s){ return String(s==null?'':s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;'); }\n  window._mmVendorChoose = function(ln, rows, typed){", 1, "ESC"], ["  <button class=\"sub-tab\" data-page=\"project\">프로젝트</button>\n", "", 1, "NOPROJ"], ["<!-- test build r187 2026-09-14 -->", "<!-- test build r188 2026-09-14 -->", 1, "MARKER"]];
  for(var i=0;i<OPS.length;i++){ var o=OPS[i]; if(!isTest && o[3]==='MARKER') continue; s = rep(s, o[0], o[1], o[2], o[3]); }
  return s;
}
if(typeof module!=='undefined') module.exports = r188Apply;
