# 작업공간 롤백 복구 가이드 (2026-08-19 갱신)

Claude 클라우드 작업공간은 세션이 쉬는 동안 예전 스냅샷으로 롤백될 수 있다.
사이트·저장소·Firebase·PC 파일은 영향 없음. 아래 절차로 작업 파일만 복구한다.

## 복구 절차
1. `git clone --depth 1 https://github.com/ChungHae/chjk-scheduler.git`
   → 배포본(live: index.html, test: test/index.html)이 곧 기준 상태.
2. 클론한 두 파일을 /mnt/user-data/outputs/index.html, testpage/index.html 로 복사.
3. 배포본 이후 진행된 수정(rNN)은 automation/rebuild/ 의 edits_rNN.py 를
   번호 순서대로 적용 (90b, 95b 같은 보조 모듈은 해당 번호 바로 뒤).
   각 모듈은 old/new 문자열 count==1 검증을 내장 — 순서가 틀리면 즉시 실패한다.
4. `git hash-object` 로 아래 해시와 대조해 바이트 단위 일치 확인.

## 해시 기록
- r77: live a62e33a6 / test b0bf625f (배포됨)
- r83: live e22fb633 / test f5c48325 (2026-08-18 배포)
- r97: live 95ce02e6 / test 3e01e5f4
- r98: live e82bc173 / test ded12fb7
- r99: live 92a73def / test 1afd85c1
- r100: live 18a86cff / test a4552fec
- r101: live 72707e31 / test 52adc8a6
- r102: live a78d8122 / test d3a49c3b
- r103: live 432b532c / test 6b1904e6
- r104: live 3f7c81e5 / test dd5b7621
- r105: live 7f2a9845 / test 9459125b
- r106: live 041617f1 / test a233ec1e
  ※ r106 = 일정 등록 기능 숨김 1단계. TEST 확인 후 완전 삭제(2단계) 예정.
- r107: live eed7d41f / test f892eb7f
- r108: live d1c46bd4 / test 4996c022
- r109: live 4900cfc0 / test dd280329
- r110: live bb85a24d / test c5d2ae8e
- r111: live 77ba5419 / test d418ceb5
- r112: live 4e0199ff / test 5c4c3320
- r113: live 5740329b / test d994b68b
- r114: live 12776761 / test 9409c4e3
- r115: live 6eac5a3b / test dbe1d176
- r116: live 18819c2e / test 7a07884c (전체 백업: automation/rebuild/backup_r116/)
- r117: live 676658ef / test 907bd19d (일정 삭제 1단계: 팀원 일정·업무 배정·팀 목표/이슈)
- r118: live 9a1109d6 / test a900452f (일정 삭제 2단계: 등록 모달·개인 목록·메모/미완료)
- r119: live a96318e7 / test 6a258fa4 (일정 삭제 3단계: 업체별 일정·분류 설정·달력 잔재)
  (미배포)
- r120: live 6eca11a7 / test f0382e16 (프로젝트 숨김 기능)
- r121: live 879fe9e8 / test b314aa2f (2026-08-21 배포, 현재 배포본)
- r122: live b3ad590e / test 4ee1a3f1 (매입매출 탭 골격)
- r123: live 4f2cec1d / test d97c65ff (누적본 이관 + _fx 블롭 분리 저장)
- r124: live 7c573131 / test db221743 (미수 현황 원장 화면)
- r125: live b89b332c / test fc67dec3 (매입·매출 집계 화면)
- r126: live 0bc35768 / test b8d0bbbe (업로드 파서: 홈택스/은행6종/어음3종 + 미배정 입금 패널)
- r127: live d06379e6 / test e0af815f (원장/미수현황/집계 엑셀 + 기초이월 병합 수정)
- r128: live 718f1b9c / test 91435a15 (디자인 통일: sub-nav 스트립·inv-toolbar·qic + 원장 차변/대변)
- r129: live 2a8c392b / test 8e540813 (전체 필터·표 전체폭·기간조회·연령분석·어음 만기)
- r130: live 93ada619 / test 7f307cae (거래 자료 초기화 + N|이름 슬롯 병합 보강)
- r131: live 2d0e5580 / test 61876369 (초기화 '별칭만 유지' + 규칙 편집 UI + 원장 엑셀 키 수정)
- r132: live f19b29f9 / test c8268819 (미배정 선택창 앱 공통 드롭다운)
- r133: live 8ebea432 / test de2ad23d (미배정 매칭 업체목록 기준 + 별칭표 관리)
- r134: live bd6f4371 / test 9593245b (규칙 5종 동기화 KEYS 버그픽스 + 복사 시 규칙 리셋 + 별칭 전체 삭제)
- r135: live a19c2311 / test 84f8c6e6 (드롭다운 '계산서에만 있는 거래처' 섹션 + 원클릭 업체 등록·배정)
- r136: live b5623185 / test 878237b6 (드롭다운 각진 모서리)
- r137: live bdc6c832 / test abf3e127 (미배정 보류(건너뛰기) + 보류 목록 재확인 흐름)
- r138: live 262bf96c / test cb2c00a2 (드롭다운 UX: 빈 검색 시 두 섹션 항상 표시)
- r139: live fef97103 / test 964273a3 (드롭다운 자가진단: 기등록 안내·타사업장 경고)
- r140: live 406c84c2 / test b2047fbc (원장 입금 건별 재배정(연필))
- r141: live 0234e626 / test 6a253ecf (교차 입금 사업장 이동 배정)
- r142: live 36b675c6 / test de8e2c46 (사명 변경+교차 입금: 동일 사업자번호 타사업장 직접 배정)
- r143: live affa17f0 / test d8f039de (카드·결제 정산 자동 제외: 카드/결+숫자, 업로드 시 + 소급 버튼)
- r144: live 15f2e7fb / test 80d90119 (자동 제외 판정 확장: 카드사명 접두+숫자4↑)
- r145: live 68c5b4e6 / test 491ae2a2 (대표자명 자동 배정 + [자동 배정 재실행] 버튼)
- r146: live c4174099 / test 704159f6 (거래처 중복 원장 버그 수정 + 중복 거래처 후보 진단·병합 도구)
- r147: live b68f46f7 / test 5c47890a (채권 연령 계산 버그 수정: 마이너스 계산서/음수 기초이월을 변제로 처리)
- r148: live e3c1e163 / test c041844b (미수현황 표 전체폭 확장 + 업체 목록 페이지네이션 교체)
- r149: live 905803bf / test 0490fd2d (업체 목록 렌더링 정지 버그 수정: allClients 손상 항목 방어)
- r150: live 19fe1cb7 / test 56566e28 (2026-08-25 — 미수현황 페이지네이션 1페이지 20개, 요약 칩은 전체 기준 유지)
- r151: live a9c50d36 / test 70ae1f35 (2026-08-26 — 매입매출 계산 속도 개선. 수치·판정 로직 불변(1,653행 대조 일치). 원장 캐시(_fxDataStamp=_fxCacheBump+배열길이; _fxSave/_fxSaveBig/_fxEnsureData/외부동기화 4곳에서 ++), _fxDupCandidates O(n²)→O(n), _fxLedgersOne 기초이월·N|병합 이름인덱스화(Object.create(null)), _fxDue 메모이제이션, 검색창 180ms 디바운스, fxArXls slice(). 첫 진입 365→28ms, 검색 344→2ms, 5글자 타이핑 1,829→0ms)
- r152: live 2895d3db / test db46801a (2026-08-26 작업본, PC test 파일 = r152, 미배포 — 매입매출 첫 진입 로딩 단축. r151 이 계산을 줄였다면 r152 는 자료 받아오는 시간을 줄임. (A) _fxEnsureData 가 블롭 6개를 for+await 로 순차 요청하던 것을 Promise.all 동시 요청으로 변경 — 왕복지연(RTT)이 6번 쌓이던 것이 1번으로. 응답 순서와 무관하게 ks 순서로 다시 담아 결과·순서 완전 동일(검증 완료). 동시호출 안전성: _fbFetch 는 호출마다 독립이고 _fxSaveBig 가 이미 _fxBlobPut 6개를 동시 발사 중. (B) _fxBlobGet 이 받은 자료를 캐시용으로 통째 JSON.parse(JSON.stringify()) 하던 깊은복사 제거(약 50ms) — 이 캐시는 _fxEnsureData(force) 에서만 읽히는데 force 가 해당 키를 먼저 delete 하므로 참조 보관으로 충분. 자료 규모 실측: 블롭 6개 합계 6.3MB(sales_서울 1,478KB 최대), JSON 파싱 13ms/깊은복사 50ms/concat 0ms → CPU 아닌 네트워크가 지배. 대역폭 공유 반영 시뮬: RTT120/100Mbps 1,373→689ms, RTT250/25Mbps 3,775→2,406ms, RTT400/10Mbps 7,893→5,814ms. 남은 시간은 6.3MB 전송 자체(세션당 1회). 더 줄이려면 매입(purch, 2.05MB=32%) 블롭을 집계 탭 진입 시로 미루는 방법이 있으나 AR 툴바 _fxMetaRefresh 의 "매입 N건" 표시·집계탭·엑셀 3곳을 함께 손봐야 해 미적용)
- r153: live 9ae0e2da / test adcecee8 (2026-08-26 — 거래처 검색 결과 정렬. 검색어가 있을 때만 0순위=이름이 검색어로 시작 / 1순위=사업자번호가 검색어로 시작 / 2순위=중간 포함 으로 묶고, 같은 순위 안에서는 기존 기준(미수 잔액 큰 순) 유지. 앞자리 판단은 _fxNormName 재사용(공백·(주)/㈜/주식회사 등 제거)이라 "케이" 검색 시 "주식회사 케이에스" 도 앞묶음. 검색에 걸리는 집합(필터)은 불변, 정렬만 변경. 미배정 입금 드롭다운(등록업체·계산서전용 두 목록)에도 동일 정렬 적용 — 60곳에서 잘리므로 순서가 중요)
- r154: live 95da4c86 / test 7f98d6fa (2026-08-26 작업본, PC test 파일 = r154, 미배포 — 미배정 입금 배정 안전장치 + 되돌리기. (1) fxAssignDep 이 후보 mm[0] 을 말없이 고르거나 후보 0곳이면 입력한 생 텍스트를 그대로 배정하던 것을 수정: 정확일치 없을 때 후보1곳=그곳 / 후보다수는 정규화 앞자리 일치가 딱 1곳이면 그곳, 아니면 후보목록 안내 후 배정 안 함 / 후보0곳은 계산서전용 거래처로 1곳 좁혀지면 fxPickNewVend(등록+배정), 아니면 안내 후 배정 안 함. 드롭다운 클릭 경로는 정확일치라 기존과 동일 동작. (2) 되돌리기: 입금 자료에 asgBat/asgAt/asgBy/asgU(이전 vendor·vbiz·held, 학습된 별칭키 a 와 별칭 이전값 pa)를 얹어 저장 — 새 동기화 키 없이 입금 블롭에 함께 저장됨. 최근 50묶음까지만 유지(_fxAsgPrune, 배정 자체는 유지). 자료 업로드 탭 상단에 "최근 거래처 배정 N건 [보기/되돌리기]" 패널 추가, 묶음 단위 되돌리기(같은 입금자명 동시배정분 포함). 별칭은 이전 값이 있었으면 그 값으로 정확히 복원, 없었으면 삭제. 자동배정 재실행(fxReassignRun)도 묶음 기록되며 별칭을 학습하지 않으므로 되돌려도 별칭 불변. fxPickCrossVend(사업장 이동 배정)는 자체 확인창이 있어 제외)
- r155: live 58afdc00 / test 7a8fe25f (2026-08-26 작업본, PC test 파일 = r155, 미배포 — 사업자번호 없는 거래처 배정 차단 + 기존 배정 점검. 근거: 미수현황은 세금계산서와 맞추는 자료이므로 사업자번호 없이는 계산서와 연결될 수 없음(사용자 지적). 단 실측으로 확인한 예외 2가지를 반영: (가) 업체목록 번호 칸이 비어도 계산서 자료에 번호가 있으면 _fxClientVbiz 가 찾아냄 (실측 333-33-33333) → 업체목록 → 계산서 순으로 조회한 뒤 판정. (나) 기초이월·조정은 계산서 없이 미수를 만듦 (실측: 번호 전무여도 기초이월만으로 2,000,000 / 조정만으로 1,500,000 이 원장에 정상 계상). 확정 규칙: 번호 찾으면 그대로 배정 / 번호 없고 기초이월·조정·계산서 근거도 없으면 배정 차단(안내) / 번호 없지만 근거 있으면 무엇 때문인지 밝히는 확인창 후 승인 시 배정. fxAssignDep 은 확인창 때문에 배정 본체를 _go() 클로저로 분리(vbiz 는 _vb 로 한 번만 계산해 재사용). fxReassignRun 도 같은 기준으로 근거 없는 건 skip + 건수 안내. 점검 패널: 자료 업로드 탭에 "사업자번호 없이 배정된 입금 N건 [보기]" — 행마다 [번호 채우기](지금 자료로 찾아질 때만)·[미배정으로](별칭도 삭제), 헤더에 [찾아지는 번호 일괄 채우기]. 목록은 _fxDataStamp 로 캐시하고 _fxVbizResolver() 로 계산서 인덱스를 한 번만 만들어 O(입금×계산서) 방지)
- r156: live dfe9aff0 / test cf33342d (2026-08-26 작업본, PC test 파일 = r156, 미배포 — 견적 자동완성 안내문이 아래 입력칸을 가리는 문제. 원인: qCartSpecKey/NameKey/EtaKey 의 open 판정이 open = (상자 보임 && _qcsCur.length) 라서, 후보 0건일 때는 안내문 상자가 떠 있는데도 open=false 가 되어 엔터가 _qcsHide 없이 qCartGridKey 로 흘러가 상자가 남았음(_qcsHide 는 Escape·항목선택에서만 호출). 수정1: 칸을 벗어나는 키(Enter/Tab/ArrowDown/ArrowUp)이고 후보 0건이면 격자 이동 전에 _qcsHide(). 글자 입력 키는 그대로라 타이핑 중 안내문 유지. 후보 있을 때 엔터=선택 동작도 불변(검증). 수정2: 바깥 클릭 감지가 data-mf=spec/name/eta 를 무조건 예외 처리해 "다른 줄 규격칸" 클릭 시에도 안 닫혔음 → _qcsShow 에서 b.__qcsOwner=el 로 기억하고 그 칸일 때만 예외)
- r157: live d9a77b13 / test cc878f87 (2026-08-26 작업본, PC test 파일 = r157, 미배포 — 전체 백업/복원에 매입매출(fx) 자료 포함. 발견: 기존 "전체 백업"이 매입매출 모듈을 통째로 누락. _fullBackupMain 에 sched_fx_alias/openings/adjusts/terms/excluded 가 없었고(구 입출금 모듈 키만 있었음), blobs 에도 _fx 블롭 6개가 없어 복원 시 계산서·입금과 미배정 매칭 작업이 전부 사라짐. 수정: (1) _fullBackupMain 에 fx 설정 5종 추가(복원은 기존 save+reloadState 경로가 그대로 처리). (2) 백업 시 _fx 블롭 6개를 Promise.all 로 동시 요청해 blobs.fx 에 저장. (3) 복원 시 _fxBlobPut 으로 되돌리고 _fxLoaded=false/_fxLoading=null/_fxBlobCache={}/_fxCacheBump++ 로 메모리 초기화. (4) 백업 완료 안내에 포함 건수 표시, 복원 확인창에 fx 포함 여부 표시. version 은 3 유지 — 새 백업 파일을 구버전(배포본 r121)에서도 형식오류 없이 복원 가능하게(비상 복원 경로 보존). 검증: 가짜 Firebase 저장소로 실제 _fxBlobPut/_fxBlobGet 경로를 통과시켜 백업→전멸→복원 왕복 PASS(매출40/매입40/입금40/조정1/제외1/별칭2/기초이월/결제조건 전부 복원). 구버전(fx 없는) 백업 파일 복원 시 기존 fx 자료 유지 확인)
- r158: live 94c28939 / test a43325db (2026-08-26 작업본, PC test 파일 = r158, 미배포 — [지점 1단계: 자료 구조 기반] clientList 항목을 [이름,번호] -> [이름,번호,지점,종사업장번호] 로 확장. 화면 동작은 안 바뀜(표시·등록규칙·일괄분류는 r159 이후). 접근자: _cliBr(미지정=서울, "화성" 포함 시 화성) / _cliBrNorm / _cliSbNorm(숫자만 남기고 1~4자리는 0채워 4자리 — 엑셀이 앞의 0을 지우는 문제 대응) / _cliSb / _cliMake / _cliKeep(이름·번호만 교체하고 지점·종사업장 보존). c[2]·c[3] 슬롯이 기존 코드에서 전혀 안 쓰이는 것 전수 확인함. clientList 변경 지점 14곳 전수 처리: ensureClientList, openAddClient(수정/추가/신규), matchExistingBizIfNeeded(개인일정·프로젝트 자동생성), qxDoUpload, clxSave(신규/수정/추가), _clxImportXlsx(신규/기존), fxPickNewVend(그 입금의 사업장을 지점으로), fxPickCrossVend(이동 대상 사업장). 항목 통째 교체하던 4곳(openAddClient·clxSave·_clxImportXlsx)이 지점을 지우던 문제를 _cliKeep 으로 차단. 엑셀: 내보내기·빈양식에 지점/종사업장번호 2열을 사업자번호 뒤에 삽입하고 스타일·열너비 인덱스 전부 재계산(NC 21->23, leftCol 0/6/7/12/16/22). 가져오기는 _CLX_HMAP 에 지점·사업장·담당지점·관리지점/종사업장번호·종사업장·종사업장코드 추가, 값이 빈 칸이면 기존 값 유지. 검증: 접근자 단위(미지정/화성지점표기/이상값/엑셀숫자12->0012 등), 변경경로 6종 보존, 엑셀 내보내기 열위치 재계산, 내보내기->다시올리기 완전 무변화, 지점열 없는 구버전 엑셀 업로드 시 기존 지점 유지 — 전부 PASS)
- r159: live 2ebe33f4 / test 1e935800 (2026-08-26 작업본, PC test 파일 = r159, 미배포 — [지점 2단계: 화면 입력·표시] (1) 업체 상세 폼: 업체명/사업자번호/대표자 줄 아래에 지점(select 서울·화성) + 종사업장번호(선택 입력) 한 줄 추가. _clxSelFld 헬퍼 신설(클래스 .clx-f 유지 -> clxSave 의 get() 이 그대로 읽음). 종사업장번호는 필수 아님 — 검증 없음, 비워도 저장(사용자 지시). 넣으면 _cliSbNorm 으로 4자리 정규화. (2) clxSave: 폼의 지점·종사업장을 읽어 저장. 폼에 그 칸이 없으면 기존 값 유지(_hasFld 방어) — 예전 화면이 떠 있는 상태에서 저장해도 지점이 지워지지 않게. (3) 업체 목록: 사업자번호 옆 "지점" 열 추가(6->7열, colgroup·thead·펼침행 colspan 동시 조정). _clxBrBadge 로 화성은 주황(#b45309)/서울은 청색(#5b7ba6) 배지, 종사업장번호 있으면 옆에 회색으로 표시. (4) 툴바에 지점 필터(전체/서울/화성, _clxBr). 필터 변경 시 1페이지 리셋·펼침 해제. 개수 표시가 "N개 업체 (화성 · 전체 M)" 로 바뀜. 페이지 진입 시 _clxBr='all' 초기화. 검증: 목록 7열·배지, 필터 3종, 폼 칸 존재·기존값 반영·placeholder, 저장(화성->서울 + 종사업장 7->0007 / 종사업장 비움 = 경고없이 공란 저장 / 신규 화성 등록 / 이름만 변경 시 화성 유지), 전 페이지 스모크 7종, 엑셀 왕복 무변화, r155 배정 차단 유지 — 전부 PASS, pageerror 0)
- r160: live be6c9435 / test 56b0f20e (2026-08-26 작업본, PC test 파일 = r160, 미배포 — [메모 기준 지점 정리 — 일회성 도구] 배경: 화성지점이 본페이지에 자기 거래처를 이미 등록하고 메모에 "화성"을 적어 구분해 둠. r159부터 등록 시 지점을 고를 수 있으니 이 메모 규칙은 한 번만 옮기고 기능 자체가 사라져야 함(사용자 지시). 설계: 후보 = 메모에 화성 포함 AND 현재 지점이 서울. 지정하면 지점이 화성이 되어 후보에서 빠지고, 후보 0곳이면 버튼·패널이 자동으로 사라짐 → 별도 플래그 없이 자연스럽게 일회성(PC/브라우저별 플래그 어긋남 없음). 메모에 화성이 있지만 지점 뜻이 아닌 업체가 남아 계속 뜨는 경우를 위해 "더 이상 보지 않기"(localStorage sched_clx_br_mig_off, 이 브라우저 한정). 안전: 관리자 전용 + _guardW, 자동변경 없음(후보를 메모 원문과 함께 보여주고 체크한 것만), 메모는 건드리지 않음, _clxMemoHi 로 메모의 화성 부분 강조. 되돌리기는 업체별 지점 재선택. 검증: 후보선별(이미 화성/메모없음 제외), 패널 기본 전체체크·메모강조, 체크해제한 업체 미변경, 지정 후 후보 감소·버튼 카운트 갱신, 마지막 후보까지 지정 시 도구 소멸, 더보지않기, 조회전용 계정 비노출 — 전부 PASS. ※ 실행 위치 주의: 메모 자료는 본페이지(정식)에 있음. TEST에는 후보가 없거나 오래된 상태일 수 있어 실제 정리는 정식 배포 후 정식에서 1회 실행해야 함)
- r161: live dae374d2 / test 596cd010 (2026-08-26 작업본, PC test 파일 = r161, 미배포 — [지점 3단계-A] 업체 상세(clientInfo) 키를 업체명 -> "업체명|지점" 으로 이관. 같은 이름 2줄 허용은 r162. 전수조사로 clientInfo 접근 26곳 확인 후 전부 처리. 헬퍼: _cliKey(행->키) / _ciKey(이름,지점->키) / _cliFind(이름[,지점]->행) / _clxInfoKey / _ciVer(상세 저장 카운터). _clxInfo(nmOrRow[,br]) 는 복합키 우선 + 옛 이름키 하위호환 조회 — 마이그레이션 전 자료나 구버전이 올린 자료도 화면이 안 빔. _ciMigrate 를 clientInfo 가 메모리에 올라오는 두 곳(선언 + reloadState)에만 감싸 최초로드·주기동기화·정식→테스트복사·백업복원 전부 커버. 이미 복합키면 그대로, 충돌 시 원본 유지(유실 방지). 유실 위험 지점 처리: (1) clxSave 는 지점만 바뀌어도 키가 달라지므로 nm!==orig 대신 복합키 비교로 이동. (2) clxMigApply 가 지점을 화성으로 바꾸면서 상세를 전혀 안 옮기던 것(최대 위험) -> 이동 추가. (3) _clxImportXlsx 는 변경 전 키를 잡아 두었다가 지점이 바뀌면 이동. (4) 삭제 3곳(deleteClient/qVendorDel/clxDelete)은 clientList 에서 지우기 "전" 에 지점을 확정해야 키를 찾을 수 있다 — 첫 구현에서 순서가 뒤바뀌어 상세가 남는 것을 테스트로 잡아 수정함. (5) _fxCeoMap 은 복합키로 읽되 색인의 값은 업체명 그대로 유지 — 복합키로 바꾸면 같은 업체가 두 지점에 있을 때 _fxResolveVendor 의 "단독 일치" 조건이 깨져 미배정이 쏟아짐. 캐시 스탬프에 _ciVer 추가(대표자만 고쳐도 갱신). 검증 8종: 마이그레이션(옛키/이미복합키/미등록 기본서울), 목록 읽기, 지점만변경, 이름만변경, 메모기준정리, 엑셀왕복 무변화, 엑셀로 지점변경, 삭제 후 상세 정리, 옛 이름키 하위호환, 대표자색인 — 전부 PASS, pageerror 0)
- r162: live 383431ee / test 61067503 (2026-08-26 작업본, PC test 파일 = r162, 미배포 — [지점 3단계-B] 같은 업체명을 서울·화성 두 줄로 등록 허용. (1) _clxExp 와 폼 data-orig 가 업체명 대신 복합키를 담는다 — 안 그러면 같은 이름 두 줄이 동시에 펼쳐지고 저장·삭제가 엉뚱한 줄을 건드림. 행에 data-key 추가, clxToggle/_clxRender 비교 전부 _cliKey 기준. _clxFormHtml(orig=복합키, pair) 는 _ciSplit 으로 이름만 뽑아 화면에 표시. (2) 중복 검사 완화: 업체명·사업자번호 모두 "같은 지점 안에서만" 막는다. _findClientByBiz(biz, except, branch) 에 지점 인자 추가(except 는 이름/복합키 둘 다 수용). (3) clxSave 머리를 재구성 — 지점 값이 필요하므로 _prev/_br/_sb/_oldKey/_newKey 를 중복검사보다 먼저 확정하고, 수정 대상 행을 이름이 아니라 _oldKey 로 찾는다. clxDelete 도 복합키로 그 줄만 삭제. (4) ensureClientList 중복 제거를 이름 -> 이름+지점 기준으로. (5) 지점 칸이 없는 옛 경로 보호: openAddClient 는 수정 시 기존 행 지점, 신규는 서울 기준으로 중복 검사. deleteClient(자동완성 삭제)/qVendorDel(견적 거래처 삭제)은 이름만 받으므로 같은 이름이 2곳 이상이면 업체 목록을 건드리지 않고 안내(잘못된 줄 삭제 방지). qVendorDel 은 견적 원장만 지우고 알림. (6) 의도적 유지: 매입매출의 _findClientByBiz(vbiz, null) 3곳은 지점 없이 그대로 — 지점을 넘기면 "서울 계좌 입금인데 업체는 화성에만 등록" 시 기존처럼 안내하지 않고 서울 줄을 새로 만들어 흐름이 크게 바뀜. 검증: 같은이름+같은번호 화성 추가 성공 / 같은지점 이름중복 차단 / 같은지점 번호중복 차단 / 두 줄 상세 분리 / 목록 두 줄·하나만 펼침·폼이 해당 지점 값 표시 / 화성만 수정 시 서울 불변 / 화성만 삭제 시 서울 잔존 / 자동완성 삭제 보호 — 전부 PASS. r161 8종·r160·스모크·엑셀왕복 회귀 통과, pageerror 0)
- r163: live b8a0d0be / test 5242a7b8 (2026-08-26 작업본, PC test 파일 = r163, 미배포 — [지점 4단계] 홈택스 계산서에서 종사업장번호 읽기 + 업체에 채우기. 순서 판단 근거: 계산서 재적재 전에 넣어야 자동으로 채워짐. 안 하면 업체마다 손입력. 견적 원장 공유·미배정 드롭다운 지점 미인식은 자료가 깨지는 문제가 아니라 뒤로 미룸. (1) 파서: 종사업장번호는 거래처 사업자번호 칸 바로 다음(매출 off+10 공급받는자 / 매입 off+5 공급자) -> iSb=iVb+1 하나로 처리. 계산서 레코드에 vsb 저장(_cliSbNorm 4자리 정규화). 기존 자료에 vsb 없어도 무영향. (2) _fxVsbOf(biz,name,vbiz): 같은 사업장 계산서 중 가장 최근 값. 사업자번호 있으면 번호 기준, 없으면 이름 기준. (3) fxPickNewVend 가 신규 업체 등록 시 종사업장번호도 함께 기록. (4) 업체 페이지 "계산서에서 종사업장번호 채우기 N" 도구(관리자 전용, r160 과 같은 검토 후 적용 방식). 후보 = 업체 종사업장 비어 있고 계산서에 '0000' 아닌 값 존재. 0000(본점)은 사실상 전 업체에 붙어 제외(계산서에는 저장됨). 채우면 후보에서 빠지고 0곳이면 버튼 소멸. "더 이상 보지 않기"(sched_clx_sb_fill_off). 검증: 실제 홈택스 열 배치로 매출·매입 파일을 파서에 통과 -> 0002/0007/0000/공란/0012 정확히 파싱, 최근값 우선(0002 -> 0007), 다른 사업장에서 조회 시 없음, 후보 선별(0000·값없음·이미있음 제외), 채우기 적용 후 버튼 소멸, 미배정 신규등록 시 자동 반영 — 전부 PASS. r160/r161/r162·스모크·엑셀왕복 회귀 통과)
- r164: live 68fc2c86 / test 9059f98e (2026-08-26 작업본, PC test 파일 = r164, 미배포 — [종사업장 여러 곳인 업체 대응] 사용자 확인: 플라즈맵처럼 사업자번호 하나에 본점+종사업장 4곳(총 5곳)인 실제 거래처가 있음. 결제는 본사가 한 번에 하므로 미수 원장은 지금처럼 합치는 게 맞음(원장 구조 불변). r163 결함: 채우기 도구가 _fxVsbOf 로 "가장 최근 값" 하나를 골라 제안 — 5곳인 업체에 0004 를 채우자고 하는 엉뚱한 값. 실제 재현 확인 후 수정. (1) _fxVsbList(biz,name,vbiz): 계산서에 나타난 종사업장번호 전부(중복제거·정렬). (2) _clxSbScan() 이 후보를 둘로 나눔 — one=하나로 확정되는 업체(자동 채우기), many=여러 곳(자동 제외, 안내만). 패널 아래에 "종사업장이 여러 곳인 업체 N곳 — 자동으로 채우지 않습니다" 구역과 값 목록 표시. 툴바 버튼 숫자는 "자동+직접선택" 형식(예: 1+1). (3) 업체 상세 폼: 종사업장번호 칸 아래에 "계산서 기준 0000 0001 …" 칩, 누르면 입력칸에 들어감(clxSbPick). (4) 미수현황 원장 상세: 계산서 줄에 "· 종사업장 0001" 표기 — 어느 사업장 건인지 보이게. 검증(플라즈맵 시나리오): 목록 [0000~0004] 정확, 자동후보는 단일업체만·플라즈맵은 직접선택으로, 채우기 적용 후 플라즈맵 미변경, 폼 칩 5개·클릭 시 0003 입력, 미수 원장 1행 잔액 12,000,000·계산서 5줄에 종사업장 표기 — 전부 PASS. r161/r162/r163·스모크·엑셀왕복 회귀 통과)
- r165: live a48ea620 / test 57803079 (2026-08-26 — **정식 배포됨** (r121 → r165). 배포 전 전수 점검(23항목 자동 점검 스위트 /tmp/h/audit.js)에서 발견한 실제 버그 수정: 업체 엑셀 가져오기가 같은 이름 2줄(서울/화성)을 구분하지 못했다. r162 에서 2줄 등록을 열었는데 가져오기의 대상 행 찾기는 그대로 이름 기준(bizMap[bd] || nameSet[exName]) 이라 엑셀 두 행이 모두 첫 줄을 대상으로 잡아 뒤 행이 앞 행을 덮었다. 재현: 한국전기 서울/0000 + 화성/0007 을 내보냈다 그대로 다시 올리면 두 줄 모두 화성/0007 이 됨(자료 소실). 수정: 대상 행을 (사업자번호|이름) + 지점 으로 찾는다 — 엑셀에 지점 값이 있으면 그 지점 줄만, 없으면 후보가 딱 하나일 때만, 후보가 여러 줄이면 건너뛰고 '건너뜀(지점 확정 불가) N행' 안내(덮어쓰기 사고 방지). 담당자 연속행(이름·번호만 있고 지점 칸이 빈 행)은 _lastNm/_lastBd/_lastKey 로 바로 앞 행에서 확정한 줄을 이어 쓴다. 갱신 루프도 이름 비교 -> 복합키(_cliKey) 비교로 교체. 점검 결과: live 23/23, test 23/23, pageerror 0. 점검 항목 = 전 페이지 스모크 9종, 원장 캐시 무효화, 마이너스 계산서 상계, 검색 정렬, 배정 차단, 배정 되돌리기, 같은이름 2줄 등록/같은지점 중복차단/두 줄 상세 분리, 종사업장 정규화, 엑셀 23열 왕복·담당자 안 섞임, 옛 이름키 이관, 종사업장 자동/직접 분리, 백업 키 전수)
  ※ r117~r119 는 마커 기반 슬라이스 삭제 모듈 — r116 이후 순서대로 적용.
    복구 지름길: backup_r116/ 파일 복사 후 117→118→119 재생.
    데이터 선언·동기화 키(assignments/comments/categories/issues/team_goal 등)는
    데이터 보호·구버전 호환 위해 코드에 유지됨.

## 주의
- edits_r62 는 _clxRender~전화포맷터 구간 슬라이스 치환이라 그 사이에 있던
  clxAddrSearch 를 삭제한다 → r62 이후 재구축 시 반드시 r66 을 다시 적용할 것.
- r67~r77 모듈은 유실 (해당 변경분은 배포본 r83에 포함되어 있어 문제 없음).
- 새 rNN 모듈을 만들면 즉시(배포를 기다리지 말고) automation/rebuild/ 에 올릴 것.
- test 파일 마커: `<!-- test build rNN 날짜 -->` (test 전용, live 에는 없음)
- 디자인 규칙: 공통 동작 버튼은 표준 아이콘(수정=연필 #5b7ba6, 삭제=휴지통 #dc2626,
  툴바 추가=+ qic)으로 통일. 임의 아이콘 금지. (r84에서 규칙화)

## 배포 방식
GitHub 웹 업로드(Whale): upload/main (live), upload/main/test (test)
→ 커밋 후 Pages 반영 ~90초 → github.io 에서 fetch 로 서빙 바이트 검증.
GitHub 커넥터는 읽기 전용(쓰기 403, Anthropic 측 알려진 제한).

## 인수인계
- 전체 프로젝트 인수인계 문서: HANDOVER.md (환경·규칙·이력·매입매출 신규 시스템 스펙 전부).
  새 세션은 HANDOVER.md 를 먼저 읽을 것.
