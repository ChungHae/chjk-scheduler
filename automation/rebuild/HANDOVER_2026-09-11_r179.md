# r179 배포 기록 (2026-09-11) — HANDOVER.md 5절을 갈음하는 최신본. 전체본은 claude.ai 프로젝트 문서 claude/HANDOVER.md 에 있음.

## 배포 상태
- **r179 배포됨** (live ae035140ff88a4cf56332adf2c119471bcfd78d9 / test 1caa0d2e898a7f0e768216a579d1c89343b70f69, 2026-09-11, 정식·test 동일 리비전).
- r179 = 메모장 기반 개인 일정. 일정 탭 서브탭 = **일정(메모장, 시작 탭)·완료(구 일정/프로젝트 화면)·주간·달력·업체**.
  흰색·전폭 패드, 왼쪽 여백에 작성 날짜, 수정·휴지통은 글 끝 바로 옆(항상 옥게 보임). 명함은 사진 첨부+이름·전화 직접 입력.
  완료 줄은 24시간 뒤 목록에서 소멸(자료 30일 뒤 정리). 요청 형식 '요청 이름 내용'.
- 모듈: automation/rebuild/edits_r179.py (로더) + r179/{css,js,markup_memo,markup_weekly}.txt + r179/apply_r179.js(JS 포팅).
  적용: r178(live f8738c92 / test 60c4426c) 에 모듈 적용 → 위 해시. 검증: playwright 66종 live·test 66/66, pageerror 0.

## 자료·코드 메모
- sched_memos(memoLines) / sched_memo_cards(memoCards). 등록 지점 6곳(선언·saveAll·doFbSave·reloadState·KEYS(live2/test3)·백업).
- _mmParse: 맨 앞 키워드 미팅/프로젝트/납품/공지/요청 → kind. 업체는 _mmMatchVendor(정확→앞글자 일치), '이름 010-…' 는 contact, _mmParseWhen(9/15, 9월 15일, 14:00, 오후 2시).
- 하위 줄: '-'/'1.' 또는 Tab → 마지막 미완료 상위 줄의 parent. 키워드 줄은 항상 상위.
- 완료(☐) → _mmCompleteOpen 창: 일정 저장(personalSchedules, src:'memo') / 프로젝트 저장(projectsList, startDate=메모 날짜, endDate=완료 날짜, 하위 줄=기록) / 담당자 등록(clientInfo[_clxInfoKey(업체)].contacts, card 필드).
- 공지(kind notice)는 전원 메모장 맨 위, 요청(to=이름)은 그 사람 '받은 요청'. 본문 필터는 UI 단(자료는 전원 동기화).
- 주간 탭(#pageWeekly 재사용): 사람×요일, personalSchedules+프로젝트 endDate+휴가. 달력은 공지 막대 제거(휴가만).
- ★ #mmList 는 통째로 다시 그린다 — 입력칸은 고정(#mmInput), 줄 수정은 그 줄에만 .mm-edit, _mmEditing 동안 재렌더 생략.
- ★ live 의 switchPage 'weekly' 블록에는 _wkMobSel 줄이 없다 — 모듈이 분기. 시작 탭 switchPage('project') 3곳 → 'memo'.
- 남은 것: 실사용 피드백 → '완료' 탭 재구성 여부(지금은 기존 프로젝트/포스트잇 화면 이름만 바꿈), 포스트잇 메모 정리 여부, 모바일 확인.

## 배포 방식 (2026-09-11부터)
- 사용자가 728haven-oss 를 협업자(Write)로 추가하고 Claude Github MCP Connector 앱을 이 저장소에 설치함.
  → 모듈·문서 같은 작은 파일은 GitHub MCP(push_files)로 바로 push. (.github/workflows 는 앱 권한에 없어 API 로 못 올림.)
- 큰 index.html 두 개(1.8MB): 모듈을 부품으로 repo 에 올린 뒤, 브라우저의 GitHub 업로드 페이지(upload/main, upload/main/test) 안에서
  api.github.com 으로 직전 리비전 blob + 부품을 받아 apply_rNNN.js 로 조립 → git blob sha 가 로컬 빌드 해시와 같을 때만
  DataTransfer 로 파일 입력에 넣고 #commit-summary-input 채워 커밋. 커밋 후 get_file_contents 로 blob sha 재확인,
  서빙은 github.io 페이지에서 XHR 로 마커 확인(github.com 에서는 CSP 때문에 github.io 를 못 불러옴).
- 배포 전 meta[name="user-login"] 이 ChungHae 인지 확인 규칙은 그대로. 이번엔 Chrome 이 ChungHae 로 로그인돼 있었음.

## RECOVERY.md 추가분
- r179: live ae035140 / test 1caa0d2e (2026-09-11 — **정식 배포됨** — 메모장 기반 개인 일정, 서브탭 일정/완료/주간/달력/업체. r178 에 edits_r179.py 적용. playwright 66/66, pageerror 0)
