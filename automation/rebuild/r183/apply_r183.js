// r183 브라우저 적용기 — edits_r183.py 의 ops_r183 에서 생성. r182(live f2500c4c / test 83f39395) 에 적용.
function r183Apply(s, isTest){
  function rep(str, old, nw, exp, label){ var n=str.split(old).length-1; if(n!==exp) throw new Error('R183 FAIL '+label+' count '+n+' (expect '+exp+')'); return str.split(old).join(nw); }
  var OPS = [["    h+='<span class=\"mm-date\">'+(opts.showDate?_mmFmtD(l.date):'')+'</span>';", "    var _de=(mine && !l.parent && !l.done && l.owner===_mmMe());   // r183: 내 상위 줄은 날짜 클릭 수정\n    h+='<span class=\"mm-date'+(opts.showDate?'':' mm-date-rep')+(_de?' mm-date-ed':'')+'\"'+(_de?(' onclick=\"mmDateEdit(\\''+l.id+'\\')\" title=\"날짜 수정\"'):'')+'>'+(l.parent?'':_mmFmtDW(l.date))+'</span>';", 1, "ROWDATE"], ["    if(mine && !l.done){\n      if(l.vendor || l.kind==='contact' || l.parent) h+='<button onclick=\"mmCardAttach(\\''+l.id+'\\')\" title=\"명함 사진 첨부\">'+_MM_SVG_CARD+'</button>';", "    if(mine && !l.done){\n      if(!l.parent) h+='<button onclick=\"mmDateEdit(\\''+l.id+'\\')\" title=\"날짜 수정\">'+_MM_SVG_CAL+'</button>';   // r183\n      if(l.vendor || l.kind==='contact' || l.parent) h+='<button onclick=\"mmCardAttach(\\''+l.id+'\\')\" title=\"명함 사진 첨부\">'+_MM_SVG_CARD+'</button>';", 1, "BTNCAL"], ["  var _MM_SVG_REPLY=", "  var _MM_SVG_CAL='<svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><rect x=\"3\" y=\"4\" width=\"18\" height=\"18\" rx=\"2\"/><path d=\"M16 2v4M8 2v4M3 10h18\"/></svg>';   // r183 날짜\n  var _MM_SVG_REPLY=", 1, "SVGCAL"], ["    var idt=document.getElementById('mmInDate'); if(idt) idt.textContent=_mmFmtD(_mmToday());", "    _mmInDateShow();   // r183", 1, "INDATE"], ["when:p.when, date:_mmToday(), createdAt:now", "when:p.when, date:(_mmInDate||_mmToday()), createdAt:now", 1, "COMMITDATE"], ["    var mine=(memoLines||[]).filter(function(l){ return l.owner===me && !l.parent && l.kind!=='notice' && _mmVisible(l, showDone); }).sort(function(a,b){ return (a.createdAt||0)-(b.createdAt||0); });", "    var mine=(memoLines||[]).filter(function(l){ return l.owner===me && !l.parent && l.kind!=='notice' && _mmVisible(l, showDone); }).sort(function(a,b){ var da=String(a.date||''), db=String(b.date||''); if(da!==db) return da<db?-1:1; return (a.createdAt||0)-(b.createdAt||0); });   // r183: 날짜→작성순", 1, "SORT"], ["if (page === 'memo') { _mmEditing=null; renderMemoPage(true);", "if (page === 'memo') { _mmEditing=null; _mmInDate=null; renderMemoPage(true);", 1, "HOOK"], ["  // ── 줄 수정 (그 줄에만 input 을 끼운다) ──", "  // ── r183: 날짜 요일 표기 + 날짜 클릭 수정 ──\n  var _mmInDate=null;   // 입력줄 날짜 지정(없으면 오늘)\n  function _mmWd(ds){ var p=String(ds||'').split('-'); if(p.length!==3) return ''; var d=new Date(+p[0],+p[1]-1,+p[2]); return ['일','월','화','수','목','금','토'][d.getDay()]||''; }\n  function _mmFmtDW(ds){ if(!ds) return ''; var w=_mmWd(ds); return _mmFmtD(ds)+(w?('('+w+')'):''); }\n  window.mmDateEdit = function(id){\n    var l=_mmFind(id); if(!l || l.done || l.parent) return;\n    if(typeof _guardW==='function' && !_guardW()) return;\n    var row=document.querySelector('#mmList .mm-row[data-id=\"'+id+'\"]'); if(!row) return;\n    var sp=row.querySelector('.mm-date'); if(!sp || sp.querySelector('input')) return;\n    _mmEditing=id;\n    sp.classList.remove('mm-date-rep'); sp.style.opacity='1';\n    sp.innerHTML='<input type=\"date\" value=\"'+_mmEsc(l.date||_mmToday())+'\">';\n    var inp=sp.querySelector('input');\n    var done=false;\n    var finish=function(save){\n      if(done) return; done=true;\n      var v=inp.value;\n      if(save && v && /^\\d{4}-\\d{2}-\\d{2}$/.test(v) && v!==l.date){\n        l.date=v; _mmChildren(l.id).forEach(function(c){ c.date=v; }); _mmSave();\n      }\n      _mmEditing=null; renderMemoPage(true);\n    };\n    inp.addEventListener('change', function(){ finish(true); });\n    inp.addEventListener('keydown', function(ev){ if(ev.key==='Escape'){ ev.preventDefault(); finish(false); } if(ev.key==='Enter'){ ev.preventDefault(); finish(true); } });\n    inp.addEventListener('blur', function(){ setTimeout(function(){ finish(true); }, 120); });\n    try{ inp.focus(); if(inp.showPicker) inp.showPicker(); }catch(_e){}\n  };\n  window.mmInDateEdit = function(){\n    var sp=document.getElementById('mmInDate'); if(!sp || sp.querySelector('input')) return;\n    var cur=_mmInDate||_mmToday();\n    sp.innerHTML='<input type=\"date\" value=\"'+cur+'\">';\n    var inp=sp.querySelector('input'); var done=false;\n    var finish=function(save){\n      if(done) return; done=true;\n      var v=inp.value;\n      if(save && v && /^\\d{4}-\\d{2}-\\d{2}$/.test(v)) _mmInDate=(v===_mmToday()?null:v);\n      sp.innerHTML=''; _mmInDateShow();\n      var mi=document.getElementById('mmInput'); if(mi){ try{ mi.focus(); }catch(_e){} }\n    };\n    inp.addEventListener('change', function(){ finish(true); });\n    inp.addEventListener('keydown', function(ev){ if(ev.key==='Escape'){ ev.preventDefault(); finish(false); } if(ev.key==='Enter'){ ev.preventDefault(); finish(true); } });\n    inp.addEventListener('blur', function(){ setTimeout(function(){ finish(true); }, 120); });\n    try{ inp.focus(); if(inp.showPicker) inp.showPicker(); }catch(_e){}\n  };\n  function _mmInDateShow(){\n    var idt=document.getElementById('mmInDate'); if(!idt || idt.querySelector('input')) return;\n    idt.textContent=_mmFmtDW(_mmInDate||_mmToday());\n    idt.classList.toggle('mm-date-set', !!_mmInDate);\n    idt.title=_mmInDate?('이 날짜로 적는 중 — 클릭해서 변경 (오늘로 돌리려면 오늘 날짜 선택)'):'클릭하면 다른 날짜로 적을 수 있습니다';\n    idt.onclick=mmInDateEdit;\n  }\n  // ── 줄 수정 (그 줄에만 input 을 끼운다) ──", 1, "JS"], ["    .page-section { display: none; }", "    /* ── r183 메모 날짜 수정·요일 ── */\n    .mm-row .mm-date.mm-date-rep { opacity:0; }\n    .mm-row:hover .mm-date.mm-date-rep { opacity:.55; }\n    .mm-date.mm-date-ed { cursor:pointer; }\n    .mm-date.mm-date-ed:hover { color:#1B3A6B; text-decoration:underline dotted; opacity:1; }\n    .mm-date input[type=date] { font-size:11px; width:100%; box-sizing:border-box; padding:1px 2px; border:1px solid #cbd5e1; border-radius:4px; font-family:inherit; color:#1f2937; background:#fff; }\n    #mmInDate { cursor:pointer; }\n    #mmInDate:hover { color:#1B3A6B; text-decoration:underline dotted; }\n    #mmInDate.mm-date-set { color:#1B3A6B; }\n    .page-section { display: none; }", 1, "CSS"], ["<!-- test build r182 2026-09-11 -->", "<!-- test build r183 2026-09-11 -->", 1, "MARKER"]];
  for(var i=0;i<OPS.length;i++){ var o=OPS[i]; if(!isTest && o[3]==='MARKER') continue; s = rep(s, o[0], o[1], o[2], o[3]); }
  return s;
}
if(typeof module!=='undefined') module.exports = r183Apply;
