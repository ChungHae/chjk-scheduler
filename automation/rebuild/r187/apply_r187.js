// r187 브라우저 적용기 — edits_r187.py 의 ops_r187 에서 생성. r186(live 20c5251c / test 286b9641) 에 적용.
function r187Apply(s, isTest){
  function rep(str, old, nw, exp, label){ var n=str.split(old).length-1; if(n!==exp) throw new Error('R187 FAIL '+label+' count '+n+' (expect '+exp+')'); return str.split(old).join(nw); }
  var OPS = [["  let smcQueue = load('sched_smc_queue') ?? [];", "  let meetings = load('sched_meetings') ?? [];     // r187 회의록 [{id,date,time,place,room,title,author,attendees,agenda:[{id,title,text,done,doneAt,doneBy}],reports:[{name,items:[...]}]}]\n  let smcQueue = load('sched_smc_queue') ?? [];", 1, "DECL"], ["    save('sched_smc_queue', smcQueue);   // r181\n", "    save('sched_smc_queue', smcQueue);   // r181\n    save('sched_meetings', meetings);   // r187\n", 1, "SAVEALL"], ["        sched_memo_cards: memoCards,\n        sched_smc_queue: smcQueue,   // r181\n", "        sched_memo_cards: memoCards,\n        sched_smc_queue: smcQueue,   // r181\n        sched_meetings: meetings,   // r187\n", 1, "FBPUT"], ["    smcQueue         = load('sched_smc_queue') ?? [];   // r181\n", "    smcQueue         = load('sched_smc_queue') ?? [];   // r181\n    meetings         = load('sched_meetings') ?? [];   // r187\n", 1, "RELOAD"], ["'sched_memos','sched_memo_cards','sched_smc_queue'", "'sched_memos','sched_memo_cards','sched_smc_queue','sched_meetings'", 3, "KEYS"], ["      sched_memos: memoLines, sched_memo_cards: memoCards,   // r179\n      sched_smc_queue: smcQueue,   // r181\n", "      sched_memos: memoLines, sched_memo_cards: memoCards,   // r179\n      sched_smc_queue: smcQueue,   // r181\n      sched_meetings: meetings,   // r187\n", 1, "BACKUP"], ["  <button class=\"sub-tab\" data-page=\"clients\">업체</button>\n  <button class=\"sub-tab\" data-page=\"project\">프로젝트</button>\n", "  <button class=\"sub-tab\" data-page=\"clients\">업체</button>\n  <button class=\"sub-tab\" data-page=\"meeting\">회의</button><!-- r187 -->\n  <button class=\"sub-tab\" data-page=\"project\">프로젝트</button>\n", 1, "SUBNAV"], ["  <section class=\"page-section\" id=\"pageMemo\">", "  <section class=\"page-section\" id=\"pageMeeting\"><!-- r187 회의록 -->\n    <div class=\"mt-tb\">\n      <span class=\"mt-title\">회의록</span>\n      <button class=\"mt-btn pri\" onclick=\"mtNew()\">+ 새 회의록</button>\n      <select id=\"mtPlaceF\" onchange=\"renderMeetingPage()\"><option value=\"\">전체</option><option value=\"서울\">서울</option><option value=\"화성\">화성</option></select>\n      <input id=\"mtQ\" placeholder=\"회의명·안건·내용 검색\" oninput=\"renderMeetingPage()\">\n      <span style=\"flex:1\"></span>\n      <span id=\"mtCnt\" style=\"font-size:12px;color:#6b7280\"></span>\n    </div>\n    <div id=\"mtBody\"></div>\n  </section>\n  <section class=\"page-section\" id=\"pageMemo\">", 1, "MARKUP"], ["    var _SCHED=['personal','weekly','biz','project','clients','memo','done'];   // r179·r184", "    var _SCHED=['personal','weekly','biz','project','clients','memo','done','meeting'];   // r179·r184·r187", 1, "SCHED"], ["const pageMap = { memo:'pageMemo', done:'pageDone',", "const pageMap = { memo:'pageMemo', done:'pageDone', meeting:'pageMeeting',", 1, "PAGEMAP"], ["    if (page === 'done') { renderDonePage(); }   // r184", "    if (page === 'done') { renderDonePage(); }   // r184\n    if (page === 'meeting') { renderMeetingPage(true); }   // r187", 1, "HOOK"], ["      else if(_id==='pageDone' && typeof renderDonePage==='function') renderDonePage();   // r184", "      else if(_id==='pageDone' && typeof renderDonePage==='function') renderDonePage();   // r184\n      else if(_id==='pageMeeting' && typeof renderMeetingPage==='function') renderMeetingPage();   // r187 (입력 중이면 건너뜀)", 1, "REFRESH"], ["  // ── 줄 수정 (그 줄에만 input 을 끼운다) ──", "\n  // ─── r187: 회의록 ───────────────────────────────────────\n  var _mtOpen=null;   // 열려 있는 회의록 id (null=목차)\n  function _mtMe(){ return (typeof _authUser!=='undefined'&&_authUser&&_authUser.name)?_authUser.name:(typeof _mmMe==='function'?_mmMe():''); }\n  function _mtSave(){ save('sched_meetings', meetings); localStorage.setItem('sched_local_ts', Date.now().toString()); try{ debouncedFbSave(); }catch(_e){} }\n  function _mtFind(id){ return (meetings||[]).find(function(m){ return m.id===id; }); }\n  function _mtId(p){ return p+Date.now().toString(36)+Math.random().toString(36).slice(2,6); }\n  function _mtEsc(s){ return String(s==null?'':s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;'); }\n  function _mtFmtD(ds){ if(!ds) return ''; var p=String(ds).split('-'); if(p.length!==3) return ds; var d=new Date(+p[0],+p[1]-1,+p[2]); var w=['일','월','화','수','목','금','토'][d.getDay()]||''; return p[0]+'. '+String(+p[1]).padStart(2,'0')+'. '+String(+p[2]).padStart(2,'0')+' ('+w+')'; }\n  function _mtFmtShort(ts){ if(!ts) return ''; var d=new Date(ts); return (d.getMonth()+1)+'/'+d.getDate(); }\n  function _mtProg(m){ var t=0, d=0; (m.agenda||[]).forEach(function(a){ t++; if(a.done) d++; }); (m.reports||[]).forEach(function(r){ (r.items||[]).forEach(function(i){ t++; if(i.done) d++; }); }); return {t:t, d:d}; }\n  function _mtDefaults(place){\n    var pl=(place==='화성')?'화성':'서울';\n    var att=(members||[]).filter(function(x){ return (x.group||'서울')===pl; }).map(function(x){ return x.name; });\n    return { place:pl, room:'충해전기㈜ '+pl+'영업소 회의실', title:'충해전기㈜ '+pl+'영업소 주간 업무회의', attendees:att };\n  }\n  function _mtEditingInside(){ var a=document.activeElement; return !!(a && a.closest && a.closest('#mtBody') && (a.tagName==='INPUT'||a.tagName==='SELECT'||a.tagName==='TEXTAREA')); }\n  window.mtNew = function(){\n    if(typeof _guardW==='function' && !_guardW()) return;\n    var me=_mtMe(); var mem=(members||[]).find(function(x){ return x.name===me; });\n    var pl=(mem&&mem.group)?mem.group:'서울';\n    var d=_mtDefaults(pl); var now=Date.now();\n    var m={ id:_mtId('mt'), date:_mmToday(), time:'09:30 ~ 10:30', place:d.place, room:d.room, title:d.title, author:me, attendees:d.attendees.slice(), agenda:[], reports:d.attendees.map(function(n){ return { name:n, items:[] }; }), createdAt:now, createdBy:me, updatedAt:now };\n    // 직전 회의(같은 장소)의 미완료 항목 가져오기\n    var prev=(meetings||[]).filter(function(x){ return x.place===m.place; }).sort(function(a,b){ return (b.date||'')<(a.date||'')?-1:1; })[0];\n    var carry=[];\n    if(prev){ (prev.agenda||[]).forEach(function(a){ if(!a.done) carry.push({sec:'agenda', a:a}); }); (prev.reports||[]).forEach(function(r){ (r.items||[]).forEach(function(i){ if(!i.done) carry.push({sec:'rep', name:r.name, a:i}); }); }); }\n    var finish=function(withCarry){\n      if(withCarry){ carry.forEach(function(c){ if(c.sec==='agenda'){ m.agenda.push({ id:_mtId('ma'), title:c.a.title||'', text:c.a.text||'', done:false, from:prev.id }); } else { var r=m.reports.find(function(x){ return x.name===c.name; }); if(!r){ r={ name:c.name, items:[] }; m.reports.push(r); if(m.attendees.indexOf(c.name)<0) m.attendees.push(c.name); } r.items.push({ id:_mtId('mi'), text:c.a.text||'', done:false, from:prev.id }); } }); }\n      meetings.push(m); _mtSave(); _mtOpen=m.id; renderMeetingPage(true);\n    };\n    if(carry.length){ showConfirmModal('이전 회의 항목', '직전 '+_mtEsc(prev.place)+' 회의('+_mtEsc(_mtFmtD(prev.date))+')의 미완료 항목 '+carry.length+'건을 새 회의록에 가져올까요?', function(){ finish(true); }, '가져오기', '#1B3A6B');\n      var ov=document.getElementById('confirmOverlay'); var cb=ov&&ov.querySelector('button.btn:not([style*=\"background\"])');\n      // 취소 시에도 새 회의록은 만든다\n      window._mtCarrySkip=function(){ finish(false); };\n      setTimeout(function(){ var ov2=document.getElementById('confirmOverlay'); if(!ov2) return; var bs=Array.from(ov2.querySelectorAll('button')); var cancel=bs.find(function(b){ return /취소|닫기/.test(b.textContent); }); if(cancel && !cancel._mtHooked){ cancel._mtHooked=true; cancel.addEventListener('click', function(){ if(window._mtCarrySkip){ var f=window._mtCarrySkip; window._mtCarrySkip=null; f(); } }); } }, 30);\n    } else finish(false);\n  };\n  window.mtOpen = function(id){ _mtOpen=id; renderMeetingPage(true); try{ window.scrollTo(0,0); }catch(_e){} };\n  window.mtBack = function(){ _mtOpen=null; renderMeetingPage(true); };\n  window.mtDelete = function(id){\n    var m=_mtFind(id); if(!m) return; if(typeof _guardW==='function' && !_guardW()) return;\n    var me=_mtMe(); var admin=(typeof _isAdmin==='function')?_isAdmin():false;\n    if(!(admin || m.createdBy===me || m.author===me)){ showInfoModal('회의록', '작성자 또는 관리자만 삭제할 수 있습니다.'); return; }\n    showConfirmModal('회의록 삭제', _mtEsc(_mtFmtD(m.date))+' '+_mtEsc(m.title)+' 회의록을 삭제할까요?', function(){ meetings=meetings.filter(function(x){ return x.id!==id; }); _mtSave(); _mtOpen=null; renderMeetingPage(true); }, '삭제', '#dc2626');\n  };\n  window.mtField = function(id, k, v){\n    var m=_mtFind(id); if(!m) return; if(typeof _guardW==='function' && !_guardW()) return;\n    v=String(v==null?'':v);\n    if(k==='place'){ var d=_mtDefaults(v); var old=_mtDefaults(m.place); m.place=d.place; if(m.room===old.room) m.room=d.room; if(m.title===old.title) m.title=d.title; }\n    else m[k]=v;\n    m.updatedAt=Date.now(); _mtSave(); renderMeetingPage(true);\n  };\n  window.mtAttToggle = function(id, name){\n    var m=_mtFind(id); if(!m) return; if(typeof _guardW==='function' && !_guardW()) return;\n    var i=m.attendees.indexOf(name);\n    if(i>=0){ m.attendees.splice(i,1); } else { m.attendees.push(name); if(!m.reports.some(function(r){ return r.name===name; })) m.reports.push({ name:name, items:[] }); }\n    m.updatedAt=Date.now(); _mtSave(); renderMeetingPage(true);\n  };\n  function _mtParseAgenda(t){ var s=String(t||'').trim(); var i=s.search(/[:：]/); if(i>0 && i<40) return { title:s.slice(0,i).trim(), text:s.slice(i+1).trim() }; return { title:'', text:s }; }\n  window.mtAddAgenda = function(id, inp){\n    var m=_mtFind(id); if(!m) return; var t=String(inp.value||'').trim(); if(!t) return; if(typeof _guardW==='function' && !_guardW()) return;\n    var p=_mtParseAgenda(t); m.agenda.push({ id:_mtId('ma'), title:p.title, text:p.text, done:false, by:_mtMe(), at:Date.now() });\n    m.updatedAt=Date.now(); _mtSave(); inp.value=''; renderMeetingPage(true);\n    setTimeout(function(){ var el=document.getElementById('mtAgIn'); if(el) el.focus(); }, 30);\n  };\n  window.mtAddRep = function(id, name, inp){\n    var m=_mtFind(id); if(!m) return; var t=String(inp.value||'').trim(); if(!t) return; if(typeof _guardW==='function' && !_guardW()) return;\n    var r=m.reports.find(function(x){ return x.name===name; }); if(!r){ r={ name:name, items:[] }; m.reports.push(r); }\n    r.items.push({ id:_mtId('mi'), text:t, done:false, by:_mtMe(), at:Date.now() });\n    m.updatedAt=Date.now(); _mtSave(); inp.value=''; renderMeetingPage(true);\n    setTimeout(function(){ var el=document.querySelector('#mtBody input[data-rep=\"'+CSS.escape(name)+'\"]'); if(el) el.focus(); }, 30);\n  };\n  function _mtItem(m, iid){ var a=(m.agenda||[]).find(function(x){ return x.id===iid; }); if(a) return a; var out=null; (m.reports||[]).forEach(function(r){ (r.items||[]).forEach(function(i){ if(i.id===iid) out=i; }); }); return out; }\n  window.mtToggle = function(id, iid){\n    var m=_mtFind(id); if(!m) return; var it=_mtItem(m, iid); if(!it) return; if(typeof _guardW==='function' && !_guardW()) return;\n    if(it.done){ it.done=false; it.doneAt=0; it.doneBy=''; } else { it.done=true; it.doneAt=Date.now(); it.doneBy=_mtMe(); }\n    m.updatedAt=Date.now(); _mtSave(); renderMeetingPage(true);\n  };\n  window.mtEditItem = function(id, iid){\n    var m=_mtFind(id); if(!m) return; var it=_mtItem(m, iid); if(!it || it.done) return; if(typeof _guardW==='function' && !_guardW()) return;\n    var cur=(it.title!=null && it.title!=='')?(it.title+': '+(it.text||'')):(it.text||'');\n    showPromptModal('항목 수정', (it.title!=null?'제목: 내용 형식으로 적을 수 있습니다.':''), cur, function(v){\n      v=String(v||'').trim(); if(!v) return;\n      if(it.title!=null){ var p=_mtParseAgenda(v); it.title=p.title; it.text=p.text; } else it.text=v;\n      m.updatedAt=Date.now(); _mtSave(); renderMeetingPage(true);\n    }, '저장');\n  };\n  window.mtDelItem = function(id, iid){\n    var m=_mtFind(id); if(!m) return; if(typeof _guardW==='function' && !_guardW()) return;\n    m.agenda=(m.agenda||[]).filter(function(x){ return x.id!==iid; });\n    (m.reports||[]).forEach(function(r){ r.items=(r.items||[]).filter(function(i){ return i.id!==iid; }); });\n    m.updatedAt=Date.now(); _mtSave(); renderMeetingPage(true);\n  };\n  function _mtItemHtml(m, it, isAgenda, print){\n    var by=it.done?('✓ '+_mtEsc(it.doneBy||'')+' '+_mtFmtShort(it.doneAt)):'';\n    var tx=(isAgenda && it.title)?('<b>'+_mtEsc(it.title)+':</b> '+_mtEsc(it.text||'')):_mtEsc(it.text||'');\n    if(print) return '<div class=\"mt-it'+(it.done?' done':'')+'\"><span class=\"mt-chk\">'+(it.done?'✓':'')+'</span><span class=\"mt-tx\">'+tx+'</span>'+(by?'<span class=\"mt-by\">'+by+'</span>':'')+'</div>';\n    return '<div class=\"mt-it'+(it.done?' done':'')+'\"><span class=\"mt-chk\" onclick=\"mtToggle(\\''+m.id+'\\',\\''+it.id+'\\')\" title=\"'+(it.done?'완료 취소':'완료')+'\">'+(it.done?'✓':'')+'</span>'\n      +'<span class=\"mt-tx\" onclick=\"mtEditItem(\\''+m.id+'\\',\\''+it.id+'\\')\" title=\"클릭하면 수정\">'+tx+'</span>'\n      +(by?'<span class=\"mt-by\">'+by+'</span>':'')+(it.from?'<span class=\"mt-by\" title=\"이전 회의에서 가져옴\">↻</span>':'')\n      +'<button class=\"mt-del\" onclick=\"mtDelItem(\\''+m.id+'\\',\\''+it.id+'\\')\" title=\"삭제\">✕</button></div>';\n  }\n  function _mtDocHtml(m, print){\n    var id=m.id;\n    var h='<div class=\"mt-doc\"><h2 class=\"mt-h\">회 의 록</h2>';\n    if(print){\n      h+='<div class=\"mt-head\"><div class=\"k\">일 시</div><div>'+_mtEsc(_mtFmtD(m.date))+' '+_mtEsc(m.time||'')+'</div><div class=\"k\">장 소</div><div>'+_mtEsc(m.room||m.place||'')+'</div>'\n        +'<div class=\"k\">회의명</div><div>'+_mtEsc(m.title||'')+'</div><div class=\"k\">작성자</div><div>'+_mtEsc(m.author||'')+'</div>'\n        +'<div class=\"k\">참석자</div><div class=\"w\">'+_mtEsc((m.attendees||[]).join(', '))+'</div></div>';\n    } else {\n      h+='<div class=\"mt-head\">'\n        +'<div class=\"k\">일 시</div><div><input type=\"date\" value=\"'+_mtEsc(m.date||'')+'\" onchange=\"mtField(\\''+id+'\\',\\'date\\',this.value)\"><input value=\"'+_mtEsc(m.time||'')+'\" placeholder=\"09:30 ~ 10:30\" style=\"width:110px\" onchange=\"mtField(\\''+id+'\\',\\'time\\',this.value)\"></div>'\n        +'<div class=\"k\">장 소</div><div><select onchange=\"mtField(\\''+id+'\\',\\'place\\',this.value)\"><option value=\"서울\"'+(m.place==='서울'?' selected':'')+'>서울</option><option value=\"화성\"'+(m.place==='화성'?' selected':'')+'>화성</option></select><input class=\"wide\" value=\"'+_mtEsc(m.room||'')+'\" onchange=\"mtField(\\''+id+'\\',\\'room\\',this.value)\"></div>'\n        +'<div class=\"k\">회의명</div><div><input class=\"wide\" value=\"'+_mtEsc(m.title||'')+'\" onchange=\"mtField(\\''+id+'\\',\\'title\\',this.value)\"></div>'\n        +'<div class=\"k\">작성자</div><div><input class=\"wide\" value=\"'+_mtEsc(m.author||'')+'\" onchange=\"mtField(\\''+id+'\\',\\'author\\',this.value)\"></div>'\n        +'<div class=\"k\">참석자</div><div class=\"w\">'+(members||[]).map(function(x){ var on=(m.attendees||[]).indexOf(x.name)>=0; return '<span class=\"mt-att'+(on?' on':'')+'\" onclick=\"mtAttToggle(\\''+id+'\\',\\''+_mtEsc(x.name)+'\\')\">'+_mtEsc(x.name)+'</span>'; }).join(' ')+'</div>'\n        +'</div>';\n    }\n    h+='<div class=\"mt-sec\"><h3>1. 공통 추진 사항 및 협의 안건</h3>';\n    if(!(m.agenda||[]).length) h+='<div style=\"font-size:12.5px;color:#9ca3af;padding:6px\">'+(print?'-':'아직 안건이 없습니다. 아래 칸에 \"제목: 내용\" 으로 적고 Enter.')+'</div>';\n    (m.agenda||[]).forEach(function(a){ h+=_mtItemHtml(m, a, true, print); });\n    if(!print) h+='<div class=\"mt-add\"><input id=\"mtAgIn\" placeholder=\"제목: 내용  (예: 신규 거래처 발굴: 담당자별 업종 분류에 따른 방문 영업 실시)  Enter\" onkeydown=\"if(event.key===\\'Enter\\'&&!event.isComposing){event.preventDefault();mtAddAgenda(\\''+id+'\\',this);}\"></div>';\n    h+='</div>';\n    h+='<div class=\"mt-sec\"><h3>2. 담당자별 업무 보고</h3>';\n    var names=(m.attendees||[]).slice(); (m.reports||[]).forEach(function(r){ if(names.indexOf(r.name)<0 && (r.items||[]).length) names.push(r.name); });\n    if(!names.length) h+='<div style=\"font-size:12.5px;color:#9ca3af;padding:6px\">참석자를 지정하면 담당자별 칸이 생깁니다.</div>';\n    names.forEach(function(n){\n      var r=(m.reports||[]).find(function(x){ return x.name===n; })||{ name:n, items:[] };\n      h+='<div class=\"mt-rep\"><div class=\"nm\">'+_mtEsc(n)+'</div><div>';\n      if(!(r.items||[]).length && print) h+='<div style=\"font-size:12.5px;color:#9ca3af\">-</div>';\n      (r.items||[]).forEach(function(i){ h+=_mtItemHtml(m, i, false, print); });\n      if(!print) h+='<div class=\"mt-add\"><input data-rep=\"'+_mtEsc(n)+'\" placeholder=\"'+_mtEsc(n)+' 업무 내용 · Enter\" onkeydown=\"if(event.key===\\'Enter\\'&&!event.isComposing){event.preventDefault();mtAddRep(\\''+id+'\\',\\''+_mtEsc(n)+'\\',this);}\"></div>';\n      h+='</div></div>';\n    });\n    h+='</div><div class=\"mt-foot\">충해전기(주) '+_mtEsc(m.place||'')+'영업소</div></div>';\n    return h;\n  }\n  window.renderMeetingPage = function(force){\n    var host=document.getElementById('mtBody'); if(!host) return;\n    if(!force && _mtEditingInside()) return;   // 입력 중이면 폴링 재렌더 건너뜀\n    var cnt=document.getElementById('mtCnt');\n    if(_mtOpen){\n      var m=_mtFind(_mtOpen); if(!m){ _mtOpen=null; }\n      else {\n        var p=_mtProg(m);\n        if(cnt) cnt.textContent = p.t ? ('항목 '+p.d+'/'+p.t+' 완료') : '';\n        host.innerHTML='<div style=\"display:flex;gap:8px;align-items:center;margin:0 auto 10px;max-width:960px\"><button class=\"mt-btn\" onclick=\"mtBack()\">← 목차</button><span style=\"flex:1\"></span><button class=\"mt-btn\" onclick=\"mtPrint(\\''+m.id+'\\')\">인쇄 / PDF</button><button class=\"mt-btn danger\" onclick=\"mtDelete(\\''+m.id+'\\')\">삭제</button></div>'+_mtDocHtml(m, false);\n        return;\n      }\n    }\n    var pf=document.getElementById('mtPlaceF'); var plf=pf?pf.value:'';\n    var qEl=document.getElementById('mtQ'); var q=_qNorm(qEl?qEl.value:'');\n    var list=(meetings||[]).filter(function(m){ if(plf && m.place!==plf) return false; if(q){ var all=[m.title,m.room].concat((m.agenda||[]).map(function(a){ return (a.title||'')+' '+(a.text||''); })).concat([].concat.apply([], (m.reports||[]).map(function(r){ return (r.items||[]).map(function(i){ return i.text||''; }); }))).join(' '); if(_qNorm(all).indexOf(q)<0) return false; } return true; })\n      .sort(function(a,b){ return (b.date||'')<(a.date||'')?-1:((b.date||'')>(a.date||'')?1:(b.createdAt||0)-(a.createdAt||0)); });\n    if(cnt) cnt.textContent = list.length ? (list.length+'건') : '';\n    if(!list.length){ host.innerHTML='<div class=\"mt-empty\">'+(q||plf?'해당하는 회의록이 없습니다.':'아직 회의록이 없습니다. [+ 새 회의록] 으로 시작하세요.')+'</div>'; return; }\n    var h='<div class=\"mt-list\"><table><thead><tr><th>No.</th><th>일시</th><th>장소</th><th>회의명</th><th>작성자</th><th>참석자</th><th>진행</th></tr></thead><tbody>';\n    list.forEach(function(m, i){ var p=_mtProg(m);\n      h+='<tr class=\"mt-row\" onclick=\"mtOpen(\\''+m.id+'\\')\"><td>'+(list.length-i)+'</td><td>'+_mtEsc(_mtFmtD(m.date))+' '+_mtEsc(m.time||'')+'</td><td>'+_mtEsc(m.place||'')+'</td><td class=\"mt-t\">'+_mtEsc(m.title||'')+'</td><td>'+_mtEsc(m.author||'')+'</td><td>'+_mtEsc((m.attendees||[]).join(', '))+'</td><td><span class=\"mt-prog'+(p.t&&p.d===p.t?' all':'')+'\">'+(p.t?(p.d+' / '+p.t):'-')+'</span></td></tr>';\n    });\n    h+='</tbody></table></div>';\n    host.innerHTML=h;\n  };\n  window.mtPrint = function(id){\n    var m=_mtFind(id); if(!m) return;\n    var pr=document.getElementById('mtPrint'); if(!pr){ pr=document.createElement('div'); pr.id='mtPrint'; document.body.appendChild(pr); }\n    pr.innerHTML=_mtDocHtml(m, true);\n    document.body.classList.add('mt-printing');\n    var done=false; var end=function(){ if(done) return; done=true; document.body.classList.remove('mt-printing'); };\n    window.addEventListener('afterprint', end, { once:true });\n    setTimeout(function(){ try{ window.print(); }catch(_e){} setTimeout(end, 1500); }, 60);\n  };\n  // ── 줄 수정 (그 줄에만 input 을 끼운다) ──", 1, "JS"], ["    .page-section { display: none; }", "    /* ── r187 회의록 ── */\n    #pageMeeting .mt-tb { display:flex; align-items:center; gap:8px; flex-wrap:wrap; padding:10px 16px; border-bottom:1px solid #e5e7eb; background:#fff; position:sticky; top:0; z-index:3; }\n    #pageMeeting .mt-tb .mt-title { font-size:14px; font-weight:700; color:#1f2937; }\n    #pageMeeting .mt-tb select, #pageMeeting .mt-tb input { padding:5px 9px; border:1px solid #d1d5db; border-radius:6px; font-size:12.5px; font-family:inherit; background:#fff; }\n    .mt-btn { padding:5px 11px; border:1px solid #d1d5db; border-radius:6px; background:#fff; font-size:12.5px; cursor:pointer; font-family:inherit; color:#374151; }\n    .mt-btn.pri { background:#1B3A6B; color:#fff; border-color:#1B3A6B; }\n    .mt-btn.danger { color:#dc2626; border-color:#fecaca; }\n    #mtBody { padding:12px 16px 60px; }\n    .mt-list table { border-collapse:collapse; width:100%; font-size:13px; }\n    .mt-list th, .mt-list td { border:1px solid #e5e7eb; padding:7px 9px; white-space:nowrap; }\n    .mt-list th { background:#f8fafc; font-weight:700; color:#374151; }\n    .mt-list tr.mt-row { cursor:pointer; } .mt-list tr.mt-row:hover td { background:#f4f6f9; }\n    .mt-list td.mt-t { white-space:normal; font-weight:600; color:#1B3A6B; }\n    .mt-prog { display:inline-block; min-width:56px; padding:1px 7px; border-radius:10px; font-size:11.5px; font-weight:700; background:#fef3c7; color:#92400e; }\n    .mt-prog.all { background:#dcfce7; color:#166534; }\n    .mt-empty { padding:40px; text-align:center; color:#9ca3af; font-size:13px; }\n    .mt-doc { max-width:960px; margin:0 auto; background:#fff; border:1px solid #e5e7eb; border-radius:10px; padding:20px 26px 30px; }\n    .mt-doc h2.mt-h { text-align:center; font-size:22px; letter-spacing:10px; color:#1B3A6B; margin:4px 0 16px; }\n    .mt-head { display:grid; grid-template-columns:78px 1fr 78px 1fr; border:1px solid #cbd5e1; font-size:12.5px; }\n    .mt-head > div { padding:6px 10px; border-bottom:1px solid #e5e7eb; border-right:1px solid #e5e7eb; display:flex; align-items:center; gap:6px; min-height:34px; flex-wrap:wrap; }\n    .mt-head > div.k { background:#eef2f7; font-weight:700; color:#1B3A6B; justify-content:center; }\n    .mt-head > div.w { grid-column:span 3; }\n    .mt-head input, .mt-head select { padding:3px 6px; border:1px solid #d1d5db; border-radius:4px; font-size:12.5px; font-family:inherit; background:#fff; }\n    .mt-head input.wide { flex:1; min-width:120px; }\n    .mt-att { display:inline-flex; align-items:center; padding:2px 9px; border-radius:12px; border:1px solid #cbd5e1; font-size:12px; cursor:pointer; color:#6b7280; background:#fff; }\n    .mt-att.on { background:#1B3A6B; color:#fff; border-color:#1B3A6B; }\n    .mt-sec { margin-top:18px; }\n    .mt-sec h3 { font-size:14px; color:#1f2937; margin:0 0 8px; padding-bottom:5px; border-bottom:2px solid #1B3A6B; }\n    .mt-it { display:flex; align-items:flex-start; gap:8px; padding:5px 6px; border-bottom:1px dashed #e5e7eb; font-size:13px; color:#1f2937; }\n    .mt-it:hover { background:#f8fafc; }\n    .mt-it .mt-chk { flex:0 0 16px; width:16px; height:16px; border:1.5px solid #94a3b8; border-radius:3px; margin-top:2px; cursor:pointer; display:inline-flex; align-items:center; justify-content:center; font-size:11px; color:#fff; }\n    .mt-it.done .mt-chk { background:#64748b; border-color:#64748b; }\n    .mt-it .mt-tx { flex:1; min-width:0; cursor:text; line-height:1.5; white-space:pre-wrap; }\n    .mt-it .mt-tx b { color:#1B3A6B; }\n    .mt-it.done .mt-tx { text-decoration:line-through; color:#9ca3af; }\n    .mt-it .mt-by { font-size:11px; color:#9ca3af; white-space:nowrap; }\n    .mt-it .mt-del { border:none; background:none; color:#dc2626; cursor:pointer; opacity:.35; padding:0 4px; font-size:13px; }\n    .mt-it:hover .mt-del { opacity:1; }\n    .mt-add { display:flex; gap:6px; margin-top:6px; }\n    .mt-add input { flex:1; padding:6px 9px; border:1px solid #cbd5e1; border-radius:6px; font-size:13px; font-family:inherit; }\n    .mt-rep { display:grid; grid-template-columns:110px 1fr; border:1px solid #cbd5e1; margin-top:6px; }\n    .mt-rep > div { padding:6px 10px; border-bottom:1px solid #e5e7eb; }\n    .mt-rep > div.nm { background:#eef2f7; font-weight:700; color:#1B3A6B; display:flex; align-items:flex-start; justify-content:center; padding-top:10px; border-right:1px solid #e5e7eb; }\n    .mt-foot { text-align:center; color:#9ca3af; font-size:11.5px; margin-top:24px; }\n    #mtPrint { display:none; }\n    @media print {\n      body.mt-printing > *:not(#mtPrint) { display:none !important; }\n      body.mt-printing #mtPrint { display:block !important; }\n      #mtPrint .mt-doc { border:none; max-width:none; padding:0; }\n      #mtPrint .mt-it { border-bottom:1px dotted #cbd5e1; page-break-inside:avoid; }\n      #mtPrint .mt-it.done .mt-tx { color:#6b7280; }\n      #mtPrint .mt-chk { border-color:#64748b; }\n    }\n    @media (max-width:640px){ .mt-head { grid-template-columns:64px 1fr; } .mt-head > div.w { grid-column:span 1; } .mt-rep { grid-template-columns:80px 1fr; } }\n    .page-section { display: none; }", 1, "CSS"], ["<!-- test build r186 2026-09-11 -->", "<!-- test build r187 2026-09-14 -->", 1, "MARKER"]];
  for(var i=0;i<OPS.length;i++){ var o=OPS[i], exp=o[2]; if(!isTest){ if(o[3]==='MARKER') continue; if(o[3]==='KEYS') exp=2; } s = rep(s, o[0], o[1], exp, o[3]); }
  return s;
}
if(typeof module!=='undefined') module.exports = r187Apply;
